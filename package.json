{
  "name": "little-jungle-chess",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "A two-player, phone-friendly Dou Shou Qi game with offline installation.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check dist/app.mjs && node --check dist/engine.mjs && node --check dist/pwa.mjs && node --check dist/sw.js",
    "start": "python3 -m http.server 4173 --directory dist"
  },
  "engines": {
    "node": ">=20"
  }
}
